###############################################################################
# Common People -- journal entries
#
# cp_je_layla -- Layla's life. Pinned by default for Egypt at game start.
# Not a goal; does not complete on success. It exists while she does.
#
# Scope: country (c:EGY). All of Layla's state lives as country variables on
# this scope, so every read here is `var:cp_X` and every write is a call to
# a scripted_effect that ultimately resolves to country-scope mutations.
#
# UI composition:
#   - Title / icon (her face as a scene image, not a portrait)
#   - Status line: 12 triggered_desc variants (4 life situations x 3 moods)
#   - Body prose: cp_je_layla_reason -- lasting "who she is" paragraphs
#   - Button: cp_layla_check_button
#   - Current-value: a monthly debug tick during Phase A verification; will
#     be removed when the pulse is proven to fire.
#
# Pulses:
#   - on_monthly_pulse: cp_debug_tick_increment + reroll cp_mood_roll
#   - on_yearly_pulse: cp_age_increment
###############################################################################

cp_je_layla = {
	icon = "gfx/interface/icons/event_icons/event_portrait.dds"

	group = je_group_internal_affairs

	# Visible monthly-ticking bar. V3 engine increments this automatically
	# each month via the monthly_progress hook on the scripted_progress_bar.
	# If the bar fills, the engine's monthly tick is firing.
	scripted_progress_bar = cp_layla_life_bar

	# Monthly-updating status. first_valid picks the first matching branch,
	# so more specific conditions (situation + mood) come before general
	# (situation only, then mood only, then default).
	status_desc = {
		first_valid = {
			# --- Ahmed at war ---
			# 36 variants total: 4 situations × 3 moods × 3 variants.
			# Each (situation × mood) slot has three Mahfouz-register
			# 3-line (thinking / doing / hoping) prose pieces that the
			# monthly variant roll (1..3) picks between.

			# --- Ahmed at war ---
			triggered_desc = { trigger = { has_variable = cp_ahmed_at_war  var:cp_mood_roll >= 7  var:cp_status_variant = 1 } desc = cp_je_layla_status_war_high }
			triggered_desc = { trigger = { has_variable = cp_ahmed_at_war  var:cp_mood_roll >= 7  var:cp_status_variant = 2 } desc = cp_je_layla_status_war_high_alt }
			triggered_desc = { trigger = { has_variable = cp_ahmed_at_war  var:cp_mood_roll >= 7  var:cp_status_variant = 3 } desc = cp_je_layla_status_war_high_alt2 }
			triggered_desc = { trigger = { has_variable = cp_ahmed_at_war  var:cp_mood_roll <= 3  var:cp_status_variant = 1 } desc = cp_je_layla_status_war_low }
			triggered_desc = { trigger = { has_variable = cp_ahmed_at_war  var:cp_mood_roll <= 3  var:cp_status_variant = 2 } desc = cp_je_layla_status_war_low_alt }
			triggered_desc = { trigger = { has_variable = cp_ahmed_at_war  var:cp_mood_roll <= 3  var:cp_status_variant = 3 } desc = cp_je_layla_status_war_low_alt2 }
			triggered_desc = { trigger = { has_variable = cp_ahmed_at_war  var:cp_status_variant = 1 } desc = cp_je_layla_status_war_mid }
			triggered_desc = { trigger = { has_variable = cp_ahmed_at_war  var:cp_status_variant = 2 } desc = cp_je_layla_status_war_mid_alt }
			triggered_desc = { trigger = { has_variable = cp_ahmed_at_war } desc = cp_je_layla_status_war_mid_alt2 }

			# --- Under the restored serfdom ---
			triggered_desc = { trigger = { has_variable = cp_under_serfdom  var:cp_mood_roll >= 7  var:cp_status_variant = 1 } desc = cp_je_layla_status_serfdom_high }
			triggered_desc = { trigger = { has_variable = cp_under_serfdom  var:cp_mood_roll >= 7  var:cp_status_variant = 2 } desc = cp_je_layla_status_serfdom_high_alt }
			triggered_desc = { trigger = { has_variable = cp_under_serfdom  var:cp_mood_roll >= 7  var:cp_status_variant = 3 } desc = cp_je_layla_status_serfdom_high_alt2 }
			triggered_desc = { trigger = { has_variable = cp_under_serfdom  var:cp_mood_roll <= 3  var:cp_status_variant = 1 } desc = cp_je_layla_status_serfdom_low }
			triggered_desc = { trigger = { has_variable = cp_under_serfdom  var:cp_mood_roll <= 3  var:cp_status_variant = 2 } desc = cp_je_layla_status_serfdom_low_alt }
			triggered_desc = { trigger = { has_variable = cp_under_serfdom  var:cp_mood_roll <= 3  var:cp_status_variant = 3 } desc = cp_je_layla_status_serfdom_low_alt2 }
			triggered_desc = { trigger = { has_variable = cp_under_serfdom  var:cp_status_variant = 1 } desc = cp_je_layla_status_serfdom_mid }
			triggered_desc = { trigger = { has_variable = cp_under_serfdom  var:cp_status_variant = 2 } desc = cp_je_layla_status_serfdom_mid_alt }
			triggered_desc = { trigger = { has_variable = cp_under_serfdom } desc = cp_je_layla_status_serfdom_mid_alt2 }

			# --- She owns the field ---
			triggered_desc = { trigger = { has_variable = cp_owns_land  var:cp_mood_roll >= 7  var:cp_status_variant = 1 } desc = cp_je_layla_status_homesteader_high }
			triggered_desc = { trigger = { has_variable = cp_owns_land  var:cp_mood_roll >= 7  var:cp_status_variant = 2 } desc = cp_je_layla_status_homesteader_high_alt }
			triggered_desc = { trigger = { has_variable = cp_owns_land  var:cp_mood_roll >= 7  var:cp_status_variant = 3 } desc = cp_je_layla_status_homesteader_high_alt2 }
			triggered_desc = { trigger = { has_variable = cp_owns_land  var:cp_mood_roll <= 3  var:cp_status_variant = 1 } desc = cp_je_layla_status_homesteader_low }
			triggered_desc = { trigger = { has_variable = cp_owns_land  var:cp_mood_roll <= 3  var:cp_status_variant = 2 } desc = cp_je_layla_status_homesteader_low_alt }
			triggered_desc = { trigger = { has_variable = cp_owns_land  var:cp_mood_roll <= 3  var:cp_status_variant = 3 } desc = cp_je_layla_status_homesteader_low_alt2 }
			triggered_desc = { trigger = { has_variable = cp_owns_land  var:cp_status_variant = 1 } desc = cp_je_layla_status_homesteader_mid }
			triggered_desc = { trigger = { has_variable = cp_owns_land  var:cp_status_variant = 2 } desc = cp_je_layla_status_homesteader_mid_alt }
			triggered_desc = { trigger = { has_variable = cp_owns_land } desc = cp_je_layla_status_homesteader_mid_alt2 }

			# --- Default: tenant's wife (most common) ---
			triggered_desc = { trigger = { var:cp_mood_roll >= 7  var:cp_status_variant = 1 } desc = cp_je_layla_status_tenant_high }
			triggered_desc = { trigger = { var:cp_mood_roll >= 7  var:cp_status_variant = 2 } desc = cp_je_layla_status_tenant_high_alt }
			triggered_desc = { trigger = { var:cp_mood_roll >= 7  var:cp_status_variant = 3 } desc = cp_je_layla_status_tenant_high_alt2 }
			triggered_desc = { trigger = { var:cp_mood_roll <= 3  var:cp_status_variant = 1 } desc = cp_je_layla_status_tenant_low }
			triggered_desc = { trigger = { var:cp_mood_roll <= 3  var:cp_status_variant = 2 } desc = cp_je_layla_status_tenant_low_alt }
			triggered_desc = { trigger = { var:cp_mood_roll <= 3  var:cp_status_variant = 3 } desc = cp_je_layla_status_tenant_low_alt2 }
			triggered_desc = { trigger = { var:cp_status_variant = 1 } desc = cp_je_layla_status_tenant_mid }
			triggered_desc = { trigger = { var:cp_status_variant = 2 } desc = cp_je_layla_status_tenant_mid_alt }
			triggered_desc = { trigger = { always = yes } desc = cp_je_layla_status_tenant_mid_alt2 }
		}
	}

	scripted_button = cp_layla_check_button

	# Monthly: 10% chance of a spontaneous reflection event. Same pool the
	# button picks from; each event has its own "seen recently" gate so
	# the same beat cannot repeat within 180 days.
	# Tick-increment and mood-reroll live on on_monthly_pulse_country →
	# cp_on_monthly (see cp_on_actions.txt) because V3 doesn't allow both
	# `effect` and `random_events` in a single on_monthly_pulse block.
	on_monthly_pulse = {
		random_events = {
			chance_to_happen = 10

			# Situation-specific
			15 = cp_layla.10   # Ahmed at war: the letter that has not come
			15 = cp_layla.11   # Homesteader: walking the field she owns
			15 = cp_layla.12   # Serfdom restored: under the bey's eye

			# Always-available village life
			10 = cp_layla.13   # A day with no wind
			10 = cp_layla.14   # The neighbour's boy in the lane
			10 = cp_layla.15   # Her mother's left hand
			10 = cp_layla.16   # The hen that will not lay
			10 = cp_layla.17   # The merchant's scale
			10 = cp_layla.18   # Friday, the river low
			10 = cp_layla.19   # The old woman who sees through walls
			10 = cp_layla.20   # The bread that did not rise
			10 = cp_layla.21   # Dust on the water
			10 = cp_layla.22   # The younger brother's letter
			10 = cp_layla.23   # The mule that will not move
		}
	}

	# Yearly: increment age.
	on_yearly_pulse = {
		effect = {
			cp_age_increment = yes
		}
	}

	weight = 100
	should_be_pinned_by_default = yes
}
